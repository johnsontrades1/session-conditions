---
quick_id: 260915-v0k
mode: quick
files_modified: [today.py]
autonomous: true
---

<objective>
Fix honesty-rule violation in the Range Envelope section: two read lines
("Size of day", "Big-range odds") narrate non-significant, immaterial
differences as findings while the base-rate table correctly dims the same
underlying stats as n.s. Gate both lines on the same sig flag the table uses,
add a 5%-relative materiality floor on top of CI significance, collapse both
lines to one honest sentence when neither clears the bar, and disclose the
floor on the page. Direction row and conditional gap row stay unchanged.
</objective>

<context>
Reported: today's NEUTRAL page shows "309 pts typical vs 311 — about 1%
smaller" and "Only 14% ran ≥1.3× ATR, against 15%" as if they were findings,
while the base-rate table dims out_range_atr/out_big_range as n.s. for the
same label. br.loc[lab, "out_range_atr_sig"] and br.loc[lab,
"out_big_range_sig"] already exist in today.py's main() — reuse them, don't
recompute.
</context>

<tasks>

<task type="auto">
  <name>Task 1: significance + materiality gating in envelope construction</name>
  <files>today.py</files>
  <action>
    In main(), inside the envelope dict construction, add:
    - size_sig = bool(br.loc[lab, "out_range_atr_sig"]); size_rel = abs(size_today_atr/size_all_atr - 1) if size_all_atr else 0; size_material = size_sig and size_rel >= 0.05
    - big_sig = bool(br.loc[lab, "out_big_range_sig"]); big_rel = abs(big_range_today/big_range_all - 1) if big_range_all else 0; big_material = big_sig and big_rel >= 0.05
    Store size_material, big_material in the envelope dict (booleans).
  </action>
  <verify>python -c "import json; e=json.load(open('docs/today.json'))['envelope']; assert 'size_material' in e and 'big_material' in e"</verify>
  <done>envelope dict carries size_material and big_material booleans derived from br's existing sig fields + 5% relative floor</done>
</task>

<task type="auto">
  <name>Task 2: render gating in envelope_html — mute, plain-word, and collapse</name>
  <files>today.py</files>
  <action>
    In envelope_html(): when size_material is False, render "Size of day: no
    different from a normal session" with the muted/n.s. visual treatment
    (reuse the existing null-result style class) instead of the quantified
    line. Same for big_material/"Big-range odds". When BOTH are False,
    replace both read-items with a single item: "Nothing about today's setup
    separates it from a typical session." (null-result style, no per-stat
    numbers). When at least one is True, render that one normally and mute
    the other in plain words. Direction row and the conditional gap row are
    unchanged. Add a materiality-floor disclosure sentence to the section's
    existing <p class="sub"> subtitle so it's always visible on the page.
  </action>
  <verify>python today.py; grep -c "no different from a normal session\|Nothing about today's setup" docs/index.html</verify>
  <done>NEUTRAL day (both non-material) shows the single collapsed sentence; materiality floor sentence present in the card subtitle</done>
</task>

</tasks>

<verification>
- [ ] python today.py renders without error, docs/index.html regenerated
- [ ] On today's (non-material) render: envelope shows the collapsed one-line read, not two quantified near-identical pairs
- [ ] Reconstructed BIG_GAP/COILED day (2026-09-14) still shows material, quantified lines (that day's stats are genuinely sig+material)
- [ ] Direction row unchanged (still states the null with CI)
- [ ] pytest tests/ -q still green
</verification>

<success_criteria>
- Same sig flags used in envelope as in the base-rate table — one honesty standard, not two
- Materiality floor documented on the page itself
- Collapse behavior verified on both a null day and a real-signal day
</success_criteria>

<output>
After completion, create .planning/quick/260915-v0k-range-envelope-significance-gating/260915-v0k-SUMMARY.md
</output>
