---
phase: 02-label-kill-keep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backtest_qqq.py, regime.py, today.py, .planning/phases/02-label-kill-keep/QQQ-BACKTEST.md]
autonomous: true
requirements: [LABEL-01, LABEL-02, LABEL-03, LABEL-04]
user_setup: []

must_haves:
  truths:
    - "Every label scored on QQQ RTH data with CI + stability; verdicts documented"
    - "TREND inversion resolved with evidence, not renamed away without replication"
    - "BIG_GAP kept or killed on real overnight gaps"
    - "Sweeps show sign stability in ±20% threshold neighborhoods (robustness, not optimization)"
  artifacts:
    - "QQQ-BACKTEST.md — full results committed"
    - "regime.py — any label/name changes with ≤2 knobs each"
  key_links:
    - "today.py page copy matches new label semantics"
---

<objective>
Kill/keep every label on QQQ RTH data (true 9:30 opens, 1999→now). LABEL-01..04.
</objective>

<context>
@.planning/phases/02-label-kill-keep/02-CONTEXT.md
@regime.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: QQQ RTH backtest run</name>
  <files>backtest_qqq.py</files>
  <action>Script runs build(load_prices("qqq_daily.csv"), load_vix()) → label counts, base_rates sig table, stability for out_range_atr + out_eff + out_gap_filled. Compare against day-1 NQ Globex results (replication check).</action>
  <verify>Script runs green, table printed</verify>
  <done>Full QQQ results in hand</done>
</task>

<task type="auto">
  <name>Task 2: Threshold robustness sweeps</name>
  <files>backtest_qqq.py</files>
  <action>For each P knob: ±20% grid (3-5 points), re-label, check each label's out_range_atr deviation keeps sign and significance band. Flag any knife-edge thresholds.</action>
  <verify>Sweep table printed per knob</verify>
  <done>Robustness documented per label</done>
</task>

<task type="auto">
  <name>Task 3: Verdicts + code changes</name>
  <files>regime.py, today.py</files>
  <action>Apply kill/keep/rename per evidence: TREND re-spec if inversion replicates (rename + page description as fade/mean-reversion condition), BIG_GAP keep/kill, others confirmed. Update page copy in today.py LABELS/description dict to match. Knob audit stays ≤2 per label.</action>
  <verify>regime.py + today.py consistent; today.py renders; pytest green</verify>
  <done>Code matches verdicts</done>
</task>

<task type="auto">
  <name>Task 4: QQQ-BACKTEST.md report</name>
  <files>.planning/phases/02-label-kill-keep/QQQ-BACKTEST.md</files>
  <action>Tables: label counts, sig matrix, stability, sweeps, per-label verdict + rationale, NQ-vs-QQQ replication notes, explicit knob count per label.</action>
  <verify>File committed</verify>
  <done>Evidence trail complete</done>
</task>

</tasks>

<verification>
- [ ] All four requirement checkboxes traceable to report sections
- [ ] regime.py label() knobs ≤2 per label
- [ ] today.py page renders with updated labels
</verification>

<success_criteria>
- LABEL-01..04 satisfied on QQQ RTH; NQ re-validation noted as deferred optional
</success_criteria>

<output>
After completion, create .planning/phases/02-label-kill-keep/02-01-SUMMARY.md
</output>
