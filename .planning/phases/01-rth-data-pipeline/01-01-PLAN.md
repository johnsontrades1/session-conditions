---
phase: 01-rth-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [fetch_databento.py, features.py, requirements.txt, tests/test_rth.py, README.md]
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]
user_setup: ["DATABENTO_API_KEY env var (databento.com signup, free $125 credit) — required only for live pull, not for build/tests"]

must_haves:
  truths:
    - "RTH daily bars aggregate from 1-minute GLBX bars over 09:30–16:00 ET only"
    - "Second run on same day pulls only missing dates (incremental)"
    - "Missing API key produces clear actionable error, not a stack trace"
    - "features.py uses RTH open when data/nq_rth_daily.csv exists, yfinance open otherwise"
  artifacts:
    - "fetch_databento.py — backfill + incremental pull with cost guard"
    - "tests/test_rth.py — aggregation, incremental logic, feature source preference, all runnable without API key"
    - "requirements.txt — pinned deps including databento"
  key_links:
    - "features.py gap features → data/nq_rth_daily.csv open column when present"
    - "fetch_databento.py output schema matches features.py expectations (lowercase cols, date index)"
---

<objective>
Build the Databento RTH pipeline: backfill NQ RTH daily bars with true 9:30 ET opens,
credit-efficient incremental daily pull, features.py preferring RTH opens. Fully
buildable and testable without a live API key (user provides key later).

Purpose: kill the Globex-open contamination flagged in the day-1 backtest (91% baseline "gap fill").
Output: fetch_databento.py, updated features.py, tests, requirements.txt.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/01-rth-data-pipeline/01-CONTEXT.md
@features.py
@fetch_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: fetch_databento.py — RTH backfill + incremental pull</name>
  <files>fetch_databento.py, requirements.txt</files>
  <read_first>fetch_data.py</read_first>
  <action>
    Create fetch_databento.py:
    - Read DATABENTO_API_KEY from env. If absent: print clear message ("export DATABENTO_API_KEY=db-... — signup at databento.com, free credit") and sys.exit(1).
    - Historical client, dataset GLBX.MDP3, schema ohlcv-1m, stype_in="continuous", symbols=["NQ.c.0"] (calendar-roll front month; comment the roll choice).
    - Backfill start 2010-06-06 (GLBX.MDP3 history start) or resume from last date in data/nq_rth_daily.csv + 1 day. If already current (last date >= last completed weekday), print "up to date" and exit 0 without any API call.
    - Cost guard: metadata.get_cost for the exact request before pulling; abort with message if > $20.
    - Aggregate to RTH daily: convert ts_event UTC → America/New_York, keep bars 09:30 ≤ t < 16:00, group by ET date: open=first, high=max, low=min, close=last, volume=sum. Pure function aggregate_rth(df_1m) → df_daily so tests can hit it without network.
    - Merge with existing CSV (dedupe on date, keep newest), write data/nq_rth_daily.csv with lowercase columns, date index — same conventions as data/nq_daily.csv.
    Create requirements.txt: yfinance, pandas, numpy, databento, pytest (pinned to installed versions). Install databento + pytest into venv.
  </action>
  <verify>venv/bin/python fetch_databento.py (no key) exits 1 with actionable message; venv/bin/python -c "from fetch_databento import aggregate_rth" works</verify>
  <acceptance_criteria>
    - fetch_databento.py contains "aggregate_rth" as importable pure function
    - No-key invocation prints signup instruction, exit code 1
    - Cost guard code path exists (grep get_cost)
  </acceptance_criteria>
  <done>Script exists, importable, key-absent path clean, deps installed</done>
</task>

<task type="auto">
  <name>Task 2: features.py prefers RTH open with yfinance fallback</name>
  <files>features.py</files>
  <read_first>features.py</read_first>
  <action>
    Load data/nq_rth_daily.csv when present; use its open (and prior close from the same
    RTH series for gap calc) for gap/open-derived features. When absent, current
    yfinance nq_daily.csv path runs unchanged. Print one line at load stating which
    open source is active ("RTH (databento)" vs "Globex (yfinance)") so regime.py runs
    are self-documenting. Keep feature/outcome separation untouched.
  </action>
  <verify>venv/bin/python regime.py still runs green on current yfinance data (fallback path)</verify>
  <acceptance_criteria>
    - features.py contains nq_rth_daily.csv preference logic
    - regime.py output unchanged when RTH file absent
  </acceptance_criteria>
  <done>Both source paths work; fallback verified against day-1 output</done>
</task>

<task type="auto">
  <name>Task 3: Tests runnable without API key</name>
  <files>tests/test_rth.py</files>
  <read_first>fetch_databento.py, features.py</read_first>
  <action>
    pytest tests with synthetic 1m bars:
    - aggregate_rth: bars outside 09:30–16:00 ET excluded; open = 09:30 bar open exactly; DST boundary date handled (use a March DST-transition week); volume summed.
    - incremental resume: given existing CSV ending date D, computed start = D+1; up-to-date case → no pull.
    - features source preference: with tmp nq_rth_daily.csv present → RTH open used; absent → yfinance open used.
  </action>
  <verify>venv/bin/python -m pytest tests/ -q → all pass</verify>
  <acceptance_criteria>
    - All tests pass without DATABENTO_API_KEY set
    - DST test present (grep -i dst tests/test_rth.py)
  </acceptance_criteria>
  <done>pytest green, no network calls</done>
</task>

<task type="auto">
  <name>Task 4: README run instructions updated</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
    Update Run section: fetch_data.py (VIX complex + fallback) then fetch_databento.py
    (RTH bars, needs DATABENTO_API_KEY). Note known-gaps section: Globex-open caveat
    resolved once RTH backfill runs; spot-check step for first live pull.
  </action>
  <verify>grep DATABENTO_API_KEY README.md</verify>
  <acceptance_criteria>
    - README documents both fetchers and key setup
  </acceptance_criteria>
  <done>README current</done>
</task>

</tasks>

<verification>
- [ ] venv/bin/python -m pytest tests/ -q passes without API key
- [ ] venv/bin/python regime.py reproduces day-1 numbers on yfinance fallback
- [ ] fetch_databento.py no-key path exits 1 with instructions
- [ ] DEFERRED (needs key): live backfill, RTH open spot-check vs known 9:30 prints, incremental second-run check
</verification>

<success_criteria>
- All tasks completed, tests green
- DATA-01 code-complete (live run deferred), DATA-02/03 verified by tests, DATA-04 unchanged via fetch_data.py
</success_criteria>

<output>
After completion, create .planning/phases/01-rth-data-pipeline/01-01-SUMMARY.md
</output>
