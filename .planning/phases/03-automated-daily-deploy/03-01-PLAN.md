---
phase: 03-automated-daily-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [today.py, run_daily.sh, docs/, README.md, ~/Library/LaunchAgents/com.johnsontrades.session-conditions.plist]
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03]
user_setup: ["GitHub Pages must serve main/docs — enabled via gh CLI if authed, else 30-sec manual toggle in repo Settings → Pages"]

must_haves:
  truths:
    - "LaunchAgent fires weekdays 7:40 AM CT and runs fetch → render → publish"
    - "Pages URL shows today's page after a successful run"
    - "Failed run leaves error in log; page date goes visibly stale"
    - "Bot commits touch docs/ only, never source code"
  artifacts:
    - "run_daily.sh — logged, fail-loud pipeline runner"
    - "~/Library/LaunchAgents/com.johnsontrades.session-conditions.plist"
    - "docs/index.html + docs/today.json (renamed from site/)"
  key_links:
    - "today.py SITE constant → docs/"
    - "run_daily.sh → git commit docs/ + push origin main"
---

<objective>
Automate the daily pre-open page: LaunchAgent → fetch → render → GitHub Pages publish,
with loud failures. DEPLOY-01..03.
</objective>

<context>
@.planning/phases/03-automated-daily-deploy/03-CONTEXT.md
@today.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: site/ → docs/ rename</name>
  <files>today.py, docs/, README.md</files>
  <action>git mv site docs; change SITE constant to docs; update README + docstring references; re-run today.py to confirm output lands in docs/.</action>
  <verify>./venv/bin/python today.py writes docs/index.html; git status shows no stray site/</verify>
  <done>docs/ is the only output dir</done>
</task>

<task type="auto">
  <name>Task 2: run_daily.sh pipeline runner</name>
  <files>run_daily.sh</files>
  <action>
    Bash script, absolute paths, logs to logs/daily.log (gitignored, timestamped lines):
    1. fetch_data.py (hard fail → log + exit)
    2. fetch_databento.py best-effort (no key → log "fallback: yfinance opens", continue; cost-guard exit 2 → log loud, continue)
    3. today.py (hard fail → log + exit)
    4. git add docs/ && commit "chore: daily page $(date +%F)" && push origin main — only when docs/ changed; never adds anything outside docs/
  </action>
  <verify>bash run_daily.sh runs end-to-end locally; log lines present; commit contains only docs/ paths</verify>
  <done>Manual invocation publishes the page</done>
</task>

<task type="auto">
  <name>Task 3: LaunchAgent weekdays 7:40 AM CT</name>
  <files>~/Library/LaunchAgents/com.johnsontrades.session-conditions.plist</files>
  <action>StartCalendarInterval array of 5 dicts (Weekday 1-5, Hour 7, Minute 40), ProgramArguments /bin/bash run_daily.sh, StandardError/Out → logs/launchd.log, load via launchctl.</action>
  <verify>launchctl list | grep session-conditions</verify>
  <done>Agent loaded, next fire Monday 7:40 CT</done>
</task>

<task type="auto">
  <name>Task 4: Enable GitHub Pages main/docs + stale-date visibility</name>
  <files>today.py</files>
  <action>gh api POST /repos/johnsontrades1/session-conditions/pages (source main /docs) if gh authed, else print manual instruction. Confirm page header shows data date prominently (already renders date — verify + make generated-timestamp visible).</action>
  <verify>curl Pages URL returns page after DNS propagates (or documented as pending)</verify>
  <done>Pages configured or user instruction emitted</done>
</task>

</tasks>

<verification>
- [ ] bash run_daily.sh end-to-end green, commit touches docs/ only
- [ ] launchctl shows loaded agent
- [ ] Pages serves docs/ (or enablement instruction delivered)
- [ ] pytest still green
</verification>

<success_criteria>
- DEPLOY-01/02/03 satisfied; Monday 7:40 CT fire is first unattended proof
</success_criteria>

<output>
After completion, create .planning/phases/03-automated-daily-deploy/03-01-SUMMARY.md
</output>
